import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  base: {
    paddingVertical: 16,
    borderRadius: 50,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    backgroundColor:"#000"
  },
  black: {
    backgroundColor:"#000",
  },

  textBase: {
    fontSize: 18,
    color:"white"
  },

  rightIcon: {
    marginLeft: 4,
  },
})
